# v0.4.9

录制条目多选的取消行为补全、划选手势语义修正、页签栏造型重做、窄窗格适配。

## 改进

- **窄窗格适配**：
  - **工具栏换行**：录制窗格太窄时，toolbar 不再挤压/重叠——页签条保持
    完整不截断，工具栏（过滤器/操作按钮）自动换行到第二行，操作按钮仍
    恒定右对齐；窗格足够宽时保持单行 30px 布局
  - **条目永不折叠**：录制条目（Console / Network / Events）在窗格太窄时
    不再压缩/截断列，摘要行保持单行完整，通过列表的横向滚动条左右划动
    查看（此前 URL/选择器/文本会按可用宽度截断省略）
- **页签栏/工具栏重做（水平对齐的稳定 toolbar）**：
  - 页签栏改为**固定单行高**（30px、永不换行）：页签块、工具栏、操作按钮在
    所有页签下保持同一水平线、逐像素不动——此前 Network 专属的工具栏
    （过滤器等）出现/消失会触发 flex 换行/行高变化，导致无关的页签块和按钮
    跟着上下微移、永远对不齐（动效锚点缺失）
  - **激活页签只改变下表面的接壤方式**：所有页签同一盒型，选中后仅将底部
    圆角改为方角、下探 1px 穿过暗线与内容区光滑熔接（顶边不动），底色加深
    （普通页签底色亦略加深）
- **划选手势不再中途取消已有选择**（文件管理器语义）：
  - 按住左键划选期间，之前的选择**保持可见**（显示为 旧选择 ∪ 划过范围），
    只在松开时结算——此前每次划过新行都会立刻清空旧选择
  - **松开结算**：普通划选替换为划过的范围；**按住 Ctrl 划选为并集**
    （旧选择完整保留）——此前 Ctrl 按住时完全无法划选

## 修复

- **多选现在可以被取消**（Console / Network / Events 通用）——此前多选一旦建立就
  一直挂着，直到切换 tab 或清空数据。现在四种方式取消多选：
  - **单击某单个条目**：取消多选（该次点击保留原有的展开/收起语义）
  - **点击面板外的任意位置**（其它 pane、工具栏、终端等）
  - **点击页面本身**：注入脚本上报页面 click（录制器既有管线新增
    `onPageInteract` 回调）→ 面板清除。这是主路径——页面是独立 webContents，
    点击它不产生面板侧 DOM 事件；且 view 通常**本来就已持有** webContents 焦点
    （点击面板行不改变 webContents 焦点），再点页面不会有任何焦点转移事件，
    只有页面内 click 上报能捕获
  - **切换 tab**（pane 不可见时清除）——覆盖键盘切换等无点击场景

  刻意**不**基于焦点事件清除：pane 的 `claimKeyboardFocus` 会在用户点击面板
  时程序化调用 `view.focus()`（键盘归还页面），若在 `onFocusGained` 里清除
  选择，会把用户刚建立/刚结算的多选立即清掉（真实点击 drawer 会触发主 DOM
  焦点变化 → split 重发 pane focus → 该链路启动——已实测定位并撤销）
- **修复：划选后第一次单击失灵** —— 真实浏览器里划选收尾的 click 落在
  mousedown/mouseup 目标的公共祖先（列表容器）上，行内的消费逻辑永远收不到，
  `suppressNextClick` 残留并把用户的下一次单击静默吞掉（既不多选取消也不展开）。
  现在每次新的 mousedown 先重置该标记

# v0.4.8

录制条目右键菜单改为 DOM 菜单——彻底消除坐标换算。

## 修复

- **条目右键菜单精确定位（结构性方案）** — 此前三版用 Electron 原生菜单，需要把
  网页 CSS 坐标换算为原生菜单坐标系，且该坐标系行为在显示缩放/多屏环境不稳定，
  反复出现错位。现在录制条目菜单改为**面板内 DOM 菜单**（`position: fixed` 直接
  取右键事件的 `clientX/Y`）：对齐由浏览器完成，**零坐标换算**，任何 DPI、窗口
  缩放、多显示器下构造性正确。菜单向右下展开而页面区域始终位于面板上/左侧，
  原生页面视图不可能覆盖它；窗口边缘自动钳位。含 复制(N 条) / 全选 两项，点击
  外部或 Esc 关闭。已在 150% 缩放下用真实鼠标右键逐点截屏验证像素级对齐。
  页面右键菜单（必须浮于原生页面视图之上）仍为原生菜单，沿用标定公式

# v0.4.7

右键菜单定位修复（真实鼠标事件标定）。

## 修复

- **右键菜单弹出位置（经 OS 级真鼠标标定）** — 用 PowerShell P/Invoke 驱动真实
  鼠标（SetCursorPos + mouse_event）、DPI 感知枚举原生菜单窗口并多点定标后确认：
  Electron `Menu.popup` 的 x/y 是**窗口相对 DIP 坐标**而非屏幕坐标。此前版本分别
  传过 DOM screenX/Y（视口相对）、屏幕 DIP（contentBounds + client×zoom）与不传
  坐标（经 remote 调用不会落到光标），全都是错误坐标系。现在录制器条目菜单按
  `client × 窗口缩放`、页面右键菜单按 `视图偏移 + 页面坐标 × 页面缩放` 弹出，
  已在 150% 显示缩放下用真实右键逐点截图验证：菜单精确出现在光标处（含页面
  缩放状态的页面菜单）

# v0.4.6

条目多选与复制、详情交互修正。

## 新功能

- **条目多选与复制**（Console / Network / Events 通用）：
  - **Ctrl+左键** 单条点选；**按住左键划动**跨行范围多选；**Shift+左键** 从锚点选到
    当前行；右键菜单另有**全选**
  - **右键菜单 → 复制**；**Ctrl+C** 快捷复制（焦点在输入框时不会劫持）
  - 复制内容按条目展开态区分：**未展开复制一行摘要**（如 `[LOG] text`、
    `GET 200 url`、`click selector = value`），**展开则复制完整细则**（Console 全文
    与元信息 / Network 全部 headers 与请求响应体 / Events 全部字段），多条以换行拼接
  - 选中行左侧蓝色竖条 + 底色高亮

## 修复

- **点击展开详情区域不再误触收起** — 展开块内的点击（含 Network 多级节标题）不再
  冒泡触发行的折叠；详情文本保持可手动划选

# v0.4.3

录制器布局位置与列表形态修正。

## 修复

- **底部模式的抽屉真正位于窗格底部** — 此前抽屉渲染在工具栏正下方（页面被挤到抽屉
  之下）；现在底部模式为「页面上、录制器下」，与 DevTools dock-bottom 一致
- **抽屉固定高度三重保险** — 高度同时以 `style.height`、内联 `flex-basis` 与
  `overflow: hidden` 三种方式约束，任何 CSS 组合都无法使其随内容自增；并新增页面
  最小空间钳制（拖拽上限保证页面区域 ≥ 约 90px）。E2E 以 150 条记录洪水验证高度
  纹丝不动。若此前仍见"内容增多覆盖整个窗格"，请确认已完全退出 Tabby（含托盘）
  后重启加载本版
- **抽屉拖拽在指针越过页面时不再卡死** — 底部抽屉顶边/右侧抽屉左缘拖向页面方向时
  指针进入原生视图区域，OS 将后续鼠标事件路由给页面导致拖拽停滞；现在拖拽期间
  自动停靠（park）原生视图（与窗格调整手势一致），松手即恢复

## 改进

- **Network 列表改为多级折叠列表** — 移除右侧详情面板；点击行原地展开多级详情：
  General / Request headers / Request body / Response headers / Response body 各为
  独立折叠节（General 默认展开，其余默认折叠），**未展开的节完全不渲染 DOM**
  （DevTools 同款懒渲染逻辑）

# v0.4.2

录制器布局与交互完善。

## 修复

- **底部抽屉高度不再随内容浮动** — 底部布局下抽屉此前实际按内容自适应高度（CSS
  flex-basis 覆盖了显式高度，录制内容多寡直接挤压页面），现在与右侧布局同一机制：
  **固定尺寸 + 手动拖拽**（底边布局拖顶边调高、右边布局拖左缘调宽），尺寸跨窗格
  记忆——隐藏再呼出、重开面板都保持你调好的大小，不再弹回默认值

## 改进

- **默认布局改为右侧抽屉**（Settings → Web Viewer 可切换 bottom / right / detached tab）
- **页签去链接化** — Console/Network/Events 不再使用超链接样式按钮，改为 UI 风格
  白色字体的页签，条目数量以淡色小字渲染
- **单条记录可下拉展开** — Console 与 Events 的每一行点击后行内展开下级详情
  （DevTools 风格）：完整文本/字段（编号、级别、时间戳、选择器、值、来源 frame 等），
  行尾箭头指示展开状态；Network 保持点击行查看右侧详情面板

# v0.4.1

录制器 UI 完善：可呼出、可选布局、页签重设计。

## 修复

- **隐藏的录制面板可重新呼出** — 此前用按钮隐藏面板后（录制仍在进行），再点录制按钮
  会直接**停止录制**，没有任何途径找回面板。现在：面板隐藏期间点击录制按钮 = 重新
  呼出面板（录制不中断）；面板可见时点击 = 开始/停止录制

## 新功能

- **录制器布局可选**（Settings → Web Viewer → Session recorder layout）：
  - **Bottom drawer（默认）** — 页面下方抽屉，顶边拖拽调高
  - **Right drawer** — 窗格右侧竖向抽屉，左缘拖拽调宽，页面自动让位
  - **Detached tab** — 分离为独立 Tabby 标签页（`Recorder — <host>`），可与 webviewer
    窗格分屏并排，等同内建 detach；关闭标签页不影响进行中的录制，录制按钮可重新
    打开它
  - 底部 ↔ 右侧切换对已打开面板即时生效；分离标签页在下次开始录制时生效
- **页签重设计** — Console/Network/Events 改为半透明扁长型小圆角 tab，浮于一条暗色
  分割线上方；活跃 tab 上角打极小半径圆角、下脚与暗线相融（线在活跃 tab 处断开，
  与录制内容连通）

# v0.4.0

新增每窗格**会话录制器**：在 Tabby 内直接观测页面的控制台、网络收发与用户交互。

## 新功能

- **会话录制器** — 工具栏新增录制按钮（红点），点击后在页面下方展开可拖拽调高的抽屉，
  含 Console / Network / Events 三个标签页，每个窗格独立一套：
  - **Console** — 捕获 `console.log/info/warn/error/debug` 与未捕获异常（标记 UNCAUGHT），
    带相对时间戳
  - **Network** — 全量请求/响应：方法、状态、URL、资源类型、大小、耗时；点击行查看
    请求/响应 headers、POST body 与响应 body（文本响应 ≤64KB 自动抓取，二进制/超大跳过）；
    重定向链自动拆分为独立条目并标注来源
  - **Events** — 点击（CSS 选择器 + 元素文本 + 坐标）、输入/变更（值，**密码框恒掩码
    `••`**）、表单提交、页面导航；跨 frame 事件标注来源域名
- **网络过滤表达式** — Network 页支持 DevTools 风格过滤：空格分隔多条件 AND、`-` 前缀
  取反、裸词 = URL 子串、`/正则/i`、以及 `method:` `status:` `type:` `domain:` `mime:`
  `has:body` 谓词（如 `api -type:image method:POST status:4xx`）。另有「静态资源」一键
  开关（默认开启，隐藏 image/font/stylesheet/script/media）。表达式写错时显示错误并保持
  不过滤，不会静默清空列表。过滤只在展示层，捕获层永远全量
- **导出** — 一键导出整个会话为 JSON（三条流 + 元信息），供外部工具分析
- **与 DevTools (F12) 自动协调** — Electron 每页面仅允许一个调试器：录制中打开 F12 时
  录制自动暂停（横幅提示），关闭 DevTools 后自动恢复，数据不清空
- **环形缓冲** — 每条流保留最近 1 万条（网络 5 千），响应 body 总量 32MB 预算，超出
  丢最旧并在面板标注丢弃数

## 已知限制

- WebSocket 流量不可见（握手不走常规请求事件）；跨站独立进程 iframe (OOPIF) 事件不覆盖
- Service Worker 代理的请求可能出现两条（与 Chrome DevTools 行为一致）
- 页面脚本可通过 `window.__tabbyWvReport` 伪造交互事件（调试工具，接受此风险）

# v0.3.2

修复快捷键分裂窗格后的两个问题：焦点落入第一个窗格地址栏、按住和弦时一次按键
级联出多个窗格。

## 修复

- **分裂窗格后焦点直达新窗格页面** — 此前用快捷键（`Ctrl+Shift+D` 等）分裂后，
  键盘焦点悬空/落回第一个窗格的 URL 输入框，接下来的按键全打进地址栏。现在
  键盘焦点跟随窗格逻辑焦点：
  - 分裂后新窗格的页面立即持有键盘焦点，可直接继续操作
  - 窗格导航（`Ctrl+Alt+方向键`）时键盘焦点随窗格移动
  - 关闭 Tabby 模态框（设置、命令面板）后焦点自动回到当前窗格页面
  - 分裂瞬间的遮挡误报不再把键盘抢给主界面（仅焦点窗格在停靠时可交接键盘，
    解除停靠时收回）
- **分裂后重按 Ctrl+Shift 不再幽灵分裂** — 焦点跟随分裂后，松开按键的 keyup
  落到新窗格而新窗格没有对应 keydown 的转发记录，全部被丢弃；热键引擎的按键
  状态残留完整和弦（2 秒内），期间仅重按 Ctrl+Shift 就会按"Ctrl+Shift+D"幽灵
  匹配、沿上次方向再次分裂。现在 keyup 转发配对改为跨窗格共享：只要任一窗格
  转发过某键的 keydown，其 keyup 无论落在哪个窗格都会送达引擎，按键状态实时
  清理。
- **按住分裂热键不再级联分裂** — 焦点跟随分裂后，按住和弦期间 OS 自动重复会
  落到新窗格页面；Chromium 对无按键历史的视图把重复标记为"全新按键"
  （`isAutoRepeat=false`），绕过原有的重复过滤，每个重复 tick 触发一次分裂。
  现在插件维护跨窗格共享的物理按键状态表：同一键未松开时的再次按下（无论
  落在哪个窗格、是否带重复标志）一律按重复丢弃，与 2s 过期机制对齐引擎语义。
  注：键盘焦点在 Tabby 主界面（终端等）时的按住重复由 tabby-hotkey-guard 拦截，
  两层防护互补。
- **`Ctrl+L` 地址栏输入修复** — 页面持有键盘时按 `Ctrl+L`，此前仅设置 DOM 焦点
  而键盘仍在页面，输入落不到地址栏；现在显式把键盘交给地址栏

# v0.3.1

与第三方热键插件共存修复。

## 修复

- **tabby-hotkey-guard 共存时转发热键失效** — 安装 tabby-hotkey-guard 后，页面聚焦
  时的全部转发组合键（分裂、窗格导航等）被静默丢弃。转发给热键引擎的合成键盘事件
  现在采用 DOM 规范的小写 `type`（与事件名一致），不再触发 guard 的重标签启发式。
  根因定界与修复建议见 HotkeyGuard 项目内的问题报告

# v0.3.0

新增页面缩放热键重定向。

## 新功能

- **页面聚焦时缩放热键作用于页面** — `Ctrl+=` / `Ctrl+-` / `Ctrl+0`（含小键盘，
  可叠加 Shift）在页面持有焦点时调整当前窗格的**页面缩放**（×1.25 步进，按站点
  持久化），行为对齐终端窗格的字体大小热键；Tabby 界面本身的缩放不受影响

# v0.2.1

## 修复

- **Profiles & connections 中出现两个"Web viewer"内置条目** — 移除了多余的模板
  profile，只保留一个内置入口

# v0.2.0

新增客户端证书（mTLS）支持与便携版安装适配。

## 新功能

- **客户端证书 (mTLS)** — 需要客户端证书的站点现在自动使用本机证书库中的匹配证书，行为对齐桌面浏览器：
  - 恰好一个匹配证书 → 自动选用并按主机记忆
  - 多个匹配证书 → 弹出 Tabby 原生选择器（证书主题 + 颁发者），选择后按主机记忆
  - 仅作用于 webviewer 窗格的请求，不影响 Tabby 其他内容

## 改进

- **便携版 (portable) 安装支持** — `npm run package:install` 现在按 `TABBY_PLUGINS_DIR` 环境变量 → 便携目录（`<exe>\data\plugins`，自动检测）→ 平台默认目录 的优先级解析安装位置

## 升级说明

从 v0.1.0 手动升级：下载下方 zip，替换插件目录中的 `tabby-webviewer` 文件夹后完全重启 Tabby。若你的 Tabby 是便携版（exe 旁有 `data\` 目录），插件目录为 `data\plugins\node_modules\`。
